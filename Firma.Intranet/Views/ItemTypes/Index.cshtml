@model IEnumerable<Firma.Data.Data.Sklep.ItemType>

@{
    ViewData["Title"] = "Typ";
}

<h1>Typ</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Dodaj</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Opis)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pozycja)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Opis)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pozycja)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.ItemTypeId">Edycja</a>
                <a asp-action="Details" class="btn btn-info" asp-route-id="@item.ItemTypeId">Szczegóły</a>
                <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.ItemTypeId">Usuń</a>
            </td>
        </tr>
    }
    </tbody>
</table>