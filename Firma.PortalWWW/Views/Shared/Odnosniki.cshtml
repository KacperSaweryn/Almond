@model IEnumerable<Firma.Data.Data.CMS.Page>
<div class="nav-wrapper container">
    <a id="logo-container" href="https://localhost:7120/" class="brand-logo" style="font-family: -apple-system">Almond</a>

    <ul class="right hide-on-med-and-down">
 
        @foreach (var item in Model)
        {
            <li>@Html.ActionLink(item.LinkTytul, "Index", "Home", new {id=item.IdStrony},null)</li>
        }
        <li>@Html.ActionLink("Sklep", "Index", "Shop")</li>


    </ul>

    <ul id="nav-mobile" class="sidenav">
        @foreach (var item in Model)
        {
            <li>@Html.ActionLink(item.LinkTytul, "Index", "Home", new {id=item.IdStrony},null)</li>
        }
        <li>@Html.ActionLink("Sklep", "Index", "Shop")</li>
    </ul>
    <a href="#" data-target="nav-mobile" class="sidenav-trigger">
        <i class="material-icons">menu</i>
    </a>

</div>